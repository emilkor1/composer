=========================== composer ===========================
Tool for triggering Airflow DAG jobs in Cloud Composer using a local CLI.
The tool can be used by for example Windows PowerShell or Cloud SDK.
GCP will authenticate the user and get the pipeline URL automatically.

The CLI will open Google Cloud and verify your GCP user before triggering any DAG jobs.

-------------------------------------------------------------------------------
To use the Python CLI, do the following:

 git clone https://github.com/emilkor1/composer.git
 cd composer/python/
 python pyComposer.py --env <ENVIRONMENT> --location <LOCATION> --dag_id <DAG_ID>
===============================================================================
